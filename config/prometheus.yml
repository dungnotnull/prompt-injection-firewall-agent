global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prompt-injection-firewall"
    static_configs:
      - targets: ["firewall:9090"]
    metrics_path: "/metrics"
